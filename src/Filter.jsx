// import {useEffect, useState} from "react";

const Filter = () => {
  //const {flags, name, population, demonym, region, capital, subregion, tld} = cardData || {};
  //const location = useLocation();
 // const cardData = location.state?.filteredData;
  //  const filterData = props.filteredData;
/*    const [data, setData] = useState([]);
    const api = "https://restcountries.com/v3.1/all";

  useEffect(() => {
    fetch(api)
      .then(response => {
        if (!response.ok) {
          throw new Error(`Network response was not ok: ${response.status}`);
        }
        return response.json();
      })
      .then(data => {
        setData(data);
      })
      .catch(error => {
        console.error('Error fetching data:', error);
      });
  }, []);
 */
  /*const { id } = useParams();
  const selectedCountry = content.find(content => content.id === parseInt(id));
*/
  return (
    <div>
        
    </div>
  )
}

export default Filter